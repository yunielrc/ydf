#
# ydf-service
#
# Manage ydoctfiles packages
#
#

#
# Constructor
#
# Returns:
#   0 on success, non-zero on error.
#
ydf::ydf_service::constructor() {
  :
}

#
# Install a ydotfile package from a directory
#
# Arguments:
#   package_dir   string     package directory
#
# Output:
#  writes installed package name to the stdout
#
# Returns:
#   0 on success, non-zero on error.
#
ydf::ydf_service::install_one_from_dir() {
  :
}

#
# Install a ydotfile package
#
# Arguments:
#   package_name   string     package name
#
# Output:
#  writes installed package name to the stdout
#
# Returns:
#   0 on success, non-zero on error.
#
ydf::ydf_service::install_one() {
  :
}

#
# Install on or many ydotfile packages
#
# Arguments:
#   packages_names   string[]   packages names
#
# Output:
#  writes installed packages names to the stdout
#
# Returns:
#   0 on success, non-zero on error.
#
ydf::ydf_service::install() {
  :
}
